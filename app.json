{
  "name": "Your Pyrogram Bot",
  "logo": "https://te.legra.ph/file/1fe7ba504e18d33aeb761.png",
  "description": "An Advanced Telegram AI Chatbot with Inline Buttons and Owner-only Broadcast Feature.",
  "repository": "https://github.com/yourusername/your-bot-repo",
  "keywords": ["telegram", "bot", "pyrogram", "chatbot", "ai", "broadcast", "mongodb"],
  "env": {
    "API_ID": {
      "description": "Your Telegram API ID (get it from my.telegram.org)",
      "required": true
    },
    "API_HASH": {
      "description": "Your Telegram API Hash (get it from my.telegram.org)",
      "required": true
    },
    "BOT_TOKEN": {
      "description": "Your Bot Token (get it from @BotFather)",
      "required": true
    },
    "MONGO_URL": {
      "description": "Your MongoDB connection URL (e.g., from MongoDB Atlas)",
      "required": true
    },
    "OWNER_ID": {
      "description": "Your Telegram User ID (numeric, for owner-only commands like broadcast). Get it from @userinfobot.",
      "required": true
    }
  },
  "formation": {
    "worker": {
      "quantity": 1,
      "size": "eco"
    }
  },
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ]
}
